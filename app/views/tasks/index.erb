<br>
<h4><%= current_user.username.capitalize %> here are your tasks:</h4>
	<ul>
		<% @tasks.each do |task| %>
	<br>
		<li><a href="/tasks/<%= task.id %>"</a><%= task.name.capitalize %>

			<form action="/tasks/<%= task.id%>/warning" method="get">
				<br>
				
				<a href="/tasks/<%= task.id %>/edit" class="btn btn-primary btn-sm">Edit</a>
				


				<input type="submit" class="btn btn-primary btn-sm" value="Delete">
			</form>
		</li>
		</br>
		<% end %>
	</ul>

